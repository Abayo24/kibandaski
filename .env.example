# Public site URL (used for canonical URLs, sitemap and structured data)
NEXT_PUBLIC_SITE_URL=https://kibandaski.co.ke

# WhatsApp number in international format, digits only (no +, no spaces)
NEXT_PUBLIC_WHATSAPP_NUMBER=254769622996

# Optional: Google Maps embed URL (Share -> Embed a map -> copy the src value)
NEXT_PUBLIC_MAP_EMBED_URL=

# Server-only secrets go here later (never prefix them with NEXT_PUBLIC_), e.g.
# DATABASE_URL=
# MPESA_CONSUMER_KEY=
# MPESA_CONSUMER_SECRET=
